"use client";
import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { Sidebar } from "@/components/layout/Sidebar";
import { RouteGuard } from "@/lib/auth/RouteGuard";
import { useAuth } from "@/lib/auth/AuthContext";
import { deleteLibraryContent, listLibrary, updateLibraryContent, type LibraryContent, type LibraryType } from "@/lib/library";
const tabs: [string, LibraryType][] = [["Bài giảng","LESSON_PLAN"],["Slide","SLIDE_DECK"],["Bài kiểm tra","TEST"],["Mô phỏng","SIMULATION"]];
const paths: Record<LibraryType,string> = { LESSON_PLAN: "/lesson-edit", SLIDE_DECK: "/slide-maker", TEST: "/library", SIMULATION: "/molecules" };
function LibraryScreen() { const { authFetch } = useAuth(); const [type,setType]=useState<LibraryType>("LESSON_PLAN"),[items,setItems]=useState<LibraryContent[]>([]),[total,setTotal]=useState(0),[q,setQ]=useState(""),[subject,setSubject]=useState(""),[sort,setSort]=useState("updatedAt"),[loading,setLoading]=useState(true),[error,setError]=useState(""),[rename,setRename]=useState<LibraryContent|null>(null),[name,setName]=useState("");
 const load=useCallback(async()=>{setLoading(true);try{const p=new URLSearchParams({type,sort,size:"50"});if(q)p.set("q",q);if(subject)p.set("subject",subject);const d=await listLibrary(authFetch,p);setItems(d.items);setTotal(d.total);setError("");}catch(e){setError(e instanceof Error?e.message:"Không thể tải thư viện.");}finally{setLoading(false);}},[authFetch,q,sort,subject,type]); useEffect(()=>{const t=setTimeout(()=>void load(),200);return()=>clearTimeout(t)},[load]); const open=(i:LibraryContent)=>`${paths[i.type]}?libraryId=${i.id}`;
 return <main className="min-h-screen bg-[#f5f1ec] text-[#2b2926]"><div className="flex min-h-screen"><Sidebar activeHref="/library"/><section className="min-w-0 flex-1 p-5 sm:p-8"><header className="flex flex-wrap justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-widest text-[#e8724a]">Content</p><h1 className="mt-1 text-3xl font-semibold">Thư viện của tôi</h1><p className="mt-2 text-sm text-[#6b6b6b]">{total} nội dung riêng tư</p></div><div className="flex gap-2"><Link className="rounded-xl border bg-white px-4 py-2 text-sm" href="/lesson-create">Tạo giáo án</Link><Link className="rounded-xl bg-[#e8724a] px-4 py-2 text-sm text-white" href="/slide-create">Tạo slide</Link><Link className="rounded-xl border bg-white px-4 py-2 text-sm" href="/molecules">Tạo mô phỏng</Link></div></header><div className="mt-7 flex border-b">{tabs.map(([l,v])=><button key={v} onClick={()=>setType(v)} className={`border-b-2 px-3 py-3 text-sm ${type===v?"border-[#e8724a] font-semibold":"border-transparent text-[#6b6b6b]"}`}>{l}</button>)}</div><div className="mt-5 flex flex-wrap gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm theo tiêu đề..." className="rounded-xl border bg-white px-3 py-2 text-sm"/><select value={subject} onChange={e=>setSubject(e.target.value)} className="rounded-xl border bg-white px-3 py-2 text-sm"><option value="">Tất cả môn</option><option value="MATH">Toán</option><option value="CHEMISTRY">Hóa học</option><option value="PHYSICS">Vật lý</option></select><select value={sort} onChange={e=>setSort(e.target.value)} className="rounded-xl border bg-white px-3 py-2 text-sm"><option value="updatedAt">Mới cập nhật</option><option value="title">Tên A–Z</option></select></div>{error&&<p className="mt-4 text-sm text-red-700">{error}</p>}{loading?<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{[1,2,3].map(x=><div key={x} className="h-44 animate-pulse rounded-2xl bg-[#e8e2db]"/>)}</div>:items.length===0?<div className="mt-8 rounded-2xl border border-dashed bg-white p-12 text-center">{type==="TEST"?"Tính năng tạo bài kiểm tra đang được phát triển.":q||subject?"Không tìm thấy nội dung phù hợp.":"Chưa có nội dung nào trong thư viện này."}</div>:<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(i=><article key={i.id} className="overflow-hidden rounded-2xl border bg-white"><Link href={open(i)} className="block h-24 bg-gradient-to-br from-[#f2ded5] to-[#eee9df] p-4 text-sm font-semibold text-[#8e4e35]">{i.type.replace("_"," ")}</Link><div className="p-4"><Link href={open(i)} className="font-semibold hover:underline">{i.title}</Link><p className="mt-2 text-xs text-[#6b6b6b]">{i.subject??"Chưa chọn môn"} · Riêng tư</p><div className="mt-4 flex gap-3 text-sm"><Link href={open(i)} className="text-[#b85c3b]">Mở</Link><button onClick={()=>{setRename(i);setName(i.title)}}>Đổi tên</button><button className="text-red-700" onClick={()=>{if(confirm(`Xóa “${i.title}”?`)) void deleteLibraryContent(authFetch,i.id).then(load).catch(e=>setError(e.message))}}>Xóa</button></div></div></article>)}</div>}{rename&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/30"><div className="w-full max-w-md rounded-2xl bg-white p-5"><h2 className="font-semibold">Đổi tên nội dung</h2><input autoFocus value={name} onChange={e=>setName(e.target.value)} className="mt-4 w-full rounded-xl border p-2"/><div className="mt-4 flex justify-end gap-2"><button onClick={()=>setRename(null)}>Hủy</button><button className="rounded-xl bg-[#e8724a] px-4 py-2 text-white" onClick={()=>{if(rename&&name.trim())void updateLibraryContent(authFetch,rename.id,{title:name.trim()}).then(()=>{setRename(null);return load()}).catch(e=>setError(e.message))}}>Lưu</button></div></div></div>}</section></div></main> }
export default function Page(){return <RouteGuard pathname="/library"><LibraryScreen/></RouteGuard>}

package com.edua.beeduasystem.domain.model.auth;

/** Chuyên môn của Teacher — chi phối màn học liệu đặc thù (ma trận 1.4.2). Null nếu không phải Teacher. */
public enum Subject {
    MATH,
    CHEMISTRY,
    PHYSICS
}

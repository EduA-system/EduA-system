# Root-level deps (husky tooling)
/node_modules/
/be/logs/

# Environment variables (credentials)
.env*

# Windows reserved name artifacts (e.g. from `2>$null` redirections)
nul

# Office temp/lock files (e.g. ~$Phu_luc_4_...doc)
~$*
